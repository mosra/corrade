# Nothing here, but not completely empty to not hit a bug in the file embedder
# on Emscripten 2.0.26 until 3.1.3 where zero-size files are reported as having
# 3 bytes. See ResourceCompileTest.cpp for details and links.
