# This is shared with PitBull to support the Test::setPreferredPlugins() case
provides=JustSomeMammal

# This is unique so we have unique mapping for
# Test::dynamicPluginFilePathConflictsWithLoadedPlugin()
provides=AGoodBoy

[data]
description=A simple dog plugin.
